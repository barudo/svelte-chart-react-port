# React Chart Component

This project is a port of a Svelte chart component to a React component.

**Location**
The React chart component lives at `src/components/Chart`.

**Install**
```sh
npm install
```

**Run**
```sh
npm run dev
```
