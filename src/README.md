job_name:
Port Svelte Component to React

job_description:
We have a component built in Svelte and need it ported to React. Codebase is attached.

The functionality should be exactly as-is demonstrated in the Svelte component but ported to React Instead.

Here is the original requirements used to build the Svelte component.

A mock-up of what to build is attached. This is a medium fidelity mock-up so you do not need to style it pixel perfect or exact/Acid Test in terms of the CSS, but it should look fairly similar. The following interactions should be supported in the UI:
- Switch between values/charts using the UI control.
- Change the time scale (x axis) by clicking the time scale control. The x-axis 'ticks' should update appropriately.
- Selecting a row causes it to render as a large chart (blue background), whereas unselected rows are in the default display mode (pink background).
- Clicking a "V" icon highlights that vertical line as depicted in the Mock-Up. This also causes all data points in that vertical line's exact time to display their tooltips.
- Hovering over any data point shows a tooltip with the value, as well.
- (optional) As you can see, when values are displayed, they should cluster or form stacks as pictured. We realize this may be difficult so it's optional.

Use the following attached test data as inputs to the UI so it renders as in the mock-up. There are data for the actual chart and the vertical "V" guides, which are visits.

The component should be contained in its' own file (or files) so it can be dropped into another application.

The deliverable is a zipped source code folder containing a react app that can be started with "npm install" and "npm start" to view the component in a web browser.

If this looks like something that would be fun for you, and this $5 test project goes well, we are interested to re-hire you for larger projects involving React component development.

To prove you're a human, please indicate in the first sentence of your response, the words "I'm Excited"